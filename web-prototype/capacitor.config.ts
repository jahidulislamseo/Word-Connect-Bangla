import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jahidul.wordconnect',
  appName: 'WordConnect Bangla',
  webDir: 'dist'
};

export default config;
